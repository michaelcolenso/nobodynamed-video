name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      render_launch:
        description: Render and stage the verified launch-six batch
        required: true
        default: false
        type: boolean

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Lint / Type / Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: astral-sh/setup-uv@v4

      - name: Install ffmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - run: uv sync

      - name: Install sidecar dependencies
        working-directory: satori-service
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: |
          uv run ruff check src tests
          uv run ruff format --check src tests

      - name: Typecheck
        run: uv run mypy --strict src

      - name: Test
        run: uv run pytest -x -q

      - name: Test and typecheck sidecar
        working-directory: satori-service
        run: pnpm test && pnpm build

  batch:
    name: Render batches
    if: github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && inputs.render_launch == true
    needs: check
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: astral-sh/setup-uv@v4

      - name: Install ffmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Install Python deps
        run: uv sync

      - name: Verify launch story approvals
        run: |
          stories=(
            stories/launch-2025/alexa-2025.yaml
            stories/launch-2025/hazel-2025.yaml
            stories/launch-2025/bertha-2025.yaml
            stories/launch-2025/kunta-2025.yaml
            stories/launch-2025/jennifer-2025.yaml
            stories/launch-2025/eleanor-2025.yaml
          )
          for story in "${stories[@]}"; do
            echo "Checking $story"
            uv run nbn story score "$story"
          done

      - name: Verify Workers AI credentials
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          if [[ -z "$CLOUDFLARE_ACCOUNT_ID" ]]; then
            echo "::error::Missing GitHub Actions secret CLOUDFLARE_ACCOUNT_ID"
            exit 1
          fi
          if [[ -z "$CLOUDFLARE_API_TOKEN" ]]; then
            echo "::error::Missing GitHub Actions secret CLOUDFLARE_API_TOKEN"
            exit 1
          fi

      - name: Install satori-service deps
        working-directory: satori-service
        run: pnpm install --frozen-lockfile

      - name: Start satori sidecar
        working-directory: satori-service
        run: pnpm exec tsx src/server.ts &
        env:
          PORT: "3001"

      - name: Wait for satori
        run: |
          for i in $(seq 1 30); do
            curl -s http://localhost:3001/health > /dev/null 2>&1 && break
            sleep 1
          done
          curl -sf http://localhost:3001/health || { echo "satori sidecar failed to start"; exit 1; }

      - name: Render verified launch batch
        # Each story must have fresh human approval bound to its current content.
        # Launch stories use checked-in verified SSA snapshots, so D1 credentials
        # are deliberately not exposed to this release job.
        run: |
          echo "::group::Rendering batches/launch-six.yaml"
          uv run nbn batch batches/launch-six.yaml
          echo "::endgroup::"
        env:
          SATORI_URL: "http://localhost:3001"
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

      - name: Upload rendered videos
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rendered-videos-${{ github.sha }}
          path: out/
          retention-days: 90
          if-no-files-found: warn

      - name: Stage verified release
        if: success()
        run: uv run python -m nobodynamed_video.release out/launch-six.summary.json out/release

      - name: Publish videos to 'videos' branch
        if: success()
        run: |
          publish_dir="$(mktemp -d)"
          cp out/release/* "$publish_dir/"
          cd "$publish_dir"
          git init
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- *.mp4 *.json
          git commit -m "Verified videos from ${{ github.sha }}"
          git -c "http.https://github.com/.extraheader=$(git -C "$GITHUB_WORKSPACE" config --get http.https://github.com/.extraheader)" push -f "https://github.com/${{ github.repository }}.git" HEAD:videos

      - name: Summary
        if: always()
        run: |
          echo "## Batch render" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Video | Size |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|------|" >> $GITHUB_STEP_SUMMARY
          shopt -s nullglob
          for f in out/*.mp4; do
            echo "| $(basename "$f" .mp4) | $(du -h "$f" | cut -f1) |" >> $GITHUB_STEP_SUMMARY
          done
